<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<!-- Titre visible dans l’onglet -->
<title>Combien de temps y me reste ?</title>
<style>
/* Fond futuriste subtil */
body {
  margin: 0;
  font-family: Arial, sans-serif;
  background-color: #111111;
  overflow-x: hidden;
  position: relative;
  height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
}

body::before {
  content: "";
  position: absolute;
  width: 200%;
  height: 200%;
  background-image: linear-gradient(90deg, rgba(255,255,255,0.05) 1px, transparent 1px),
                    linear-gradient(rgba(255,255,255,0.05) 1px, transparent 1px);
  background-size: 50px 50px;
  animation: moveLines 10s linear infinite;
  z-index: 0;
}

@keyframes moveLines {
  0% { transform: translate(0,0); }
  100% { transform: translate(-50px, -50px); }
}

/* Conteneur principal */
.countdown-container {
  position: relative;
  z-index: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background-color: rgba(17,17,17,0.9);
  padding: 25px;
  border-radius: 16px;
  width: 90%;
  max-width: 350px;
  box-shadow: 0 0 20px rgba(255,255,255,0.05);
  text-align: center;
}

/* Temps restant */
.time-remaining {
  font-size: 3rem;
  color: #FFFFFF;
  margin-bottom: 10px;
  word-break: break-word;
}

/* Heure cible */
.target-time {
  font-size: 1.2rem;
  color: #9CA3AF;
  margin-bottom: 20px;
}

/* Boutons */
.buttons {
  display: flex;
  flex-direction: column;
  gap: 12px;
  width: 100%;
}

.buttons button {
  padding: 14px;
  border: none;
  border-radius: 10px;
  font-size: 1rem;
  font-weight: bold;
  cursor: pointer;
  transition: opacity 0.2s ease, transform 0.1s ease;
}

.buttons button:hover,
.buttons button:active {
  opacity: 0.85;
  transform: scale(0.98);
}

/* Couleurs selon priorité */
.btn-primary { background-color: #3B82F6; color: #FFFFFF; }
.btn-secondary { background-color: #22C55E; color: #FFFFFF; }
.btn-tertiary { background-color: #374151; color: #FFFFFF; }

/* Responsive iPhone */
@media (max-width: 400px) {
  .time-remaining { font-size: 2.4rem; }
  .target-time { font-size: 1rem; }
  .buttons button { padding: 12px; font-size: 0.95rem; }
}
</style>
</head>
<body>
  <div class="countdown-container">
    <div class="time-remaining" id="timeRemaining">23:57:57</div>
    <div class="target-time" id="targetTime">CIBLE : 02:18</div>
    <div class="buttons">
      <button class="btn-primary" onclick="choisirHeure()">CHOISIR L’HEURE</button>
      <button class="btn-secondary" onclick="startCountdown()">START</button>
      <button class="btn-tertiary" onclick="resetCountdown()">RESET</button>
    </div>
  </div>

  <script>
    // Fonctions déjà branchées
    function choisirHeure() {
      if (typeof openTargetPicker === 'function') { openTargetPicker(); }
    }
    function startCountdown() {
      if (typeof startExistingCountdown === 'function') { startExistingCountdown(); }
    }
    function resetCountdown() {
      if (typeof resetExistingCountdown === 'function') { resetExistingCountdown(); }
    }
  </script>
</body>
</html>
